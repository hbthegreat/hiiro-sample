export const environment = {
  production: true,
  apiUrl: '/api/hiiro',                   // Backend proxy on the same domain
  hiiroApiUrl: 'https://api.fsco.io/v2'   // Production Hiiro API URL (for reference only)
}; 